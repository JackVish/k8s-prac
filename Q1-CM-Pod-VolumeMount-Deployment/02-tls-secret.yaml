apiVersion: v1
kind: Secret
metadata:
  name: nginx-tls
  namespace: nginx-static
type: kubernetes.io/tls
data:
  # These are placeholder values. Replace with your own base64‑encoded certificate
  # and key, or generate a self‑signed cert and create the secret via kubectl.
  tls.crt: REPLACE_WITH_BASE64_CERT
  tls.key: REPLACE_WITH_BASE64_KEY
