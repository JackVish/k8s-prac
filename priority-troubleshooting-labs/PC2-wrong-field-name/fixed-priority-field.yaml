apiVersion: v1
kind: Pod
metadata:
  name: fixed-field
spec:
  # Correct field name
  priorityClassName: high-priority-lab
  containers:
  - name: nginx
    image: nginx:1.25
    resources:
      requests:
        cpu: "100m"
        memory: "128Mi"